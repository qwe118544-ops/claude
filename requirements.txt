# Core (required)
numpy>=1.24
pandas>=2.0

# Live Open-Meteo source (optional — only needed for --source openmeteo)
requests>=2.28

# Reliability-diagram PNG (optional — text diagram works without it)
matplotlib>=3.6
